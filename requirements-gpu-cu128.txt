# Install this before requirements.txt when you want NVIDIA GPU PyTorch.
# Example:
#   pip install -r requirements-gpu-cu128.txt
#   pip install -r requirements.txt
--index-url https://download.pytorch.org/whl/cu128
torch==2.10.0
